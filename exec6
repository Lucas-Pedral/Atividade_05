#include <stdio.h>

int main(){
    int numero[10];
    int valor, encontrado = 0;
    
    for (int i = 0; i < 10; i++){
        printf("Digite os núemeros para saber se o numero existe ");
        scanf("%d", &numero[i]);
        
        numero[i] = encontrado;
        }
        
        printf("Digite o valor para encontrar ");
        scanf("%d", &valor);
        for (int i = 0; i < 10; i++)
        if (numero[i] == valor){
            encontrado = 1;
        }
        
        if (encontrado){
            printf("Valor encontrado");
        }
        else{
            printf("Valor não encontrado");
        }
   return 0; 
}
